queries:
  - id: half-life-3
    description: Half-Life 3 announcement
    query: Half-Life 3 is officially announced by Valve
    active: true
    # interval: 1d   ← daily is the default; omit to keep daily

  - id: ps5-price-drop
    description: PS5 price drop
    query: the PS5 gets an official price drop
    active: true
    interval: 1w    # check weekly; options: 1d (default), 1w, 1mo
